# seaweed

個人向けの生活資金・固定費・投資記録の Web アプリケーション。利用者は 1 名。

## 正本

| 優先 | 場所 | 役割 |
| --- | --- | --- |
| 1 | `docs/requirements/要件定義書.md` | 何を作るか |
| 2 | `docs/design/*.md` | どう作るか（揃ってから実装） |
| 3 | 実装コード | 設計の後に置く。現時点では未作成 |

`docs/requirements/prompt.md` は要件定義書の再生成用入力。実装の正ではない。
`docs/design/設計成果物一覧.md` は設計書の目録。`docs/design/生成指示方針.md` は設計書作成の依頼テンプレート。

## スタック（要件）

React / Java (Spring Boot) / MySQL。本番は AWS EC2 にアプリと MySQL を同居。到達は Tailscale 閉域。RDS・公開 HTTPS・許可 IP リストは使わない。

## エージェントへの指示

- 設計書は成果物一覧の番号順に、1 指示 1 ファイル。他ファイルや DDL・コードを同時に出さない。
- 要件にない機能を足さない。対象外（AI 分析、株価自動取得、パスワード変更 UI、公開 TLS 等）を設計で復活させない。
- コーディングは設計書が揃ってから、設計どおりに行う。衝突時はコードを書く前に差分を出す。
- 秘密情報をコミットしない。
- 過去の Python 実装や旧 React ツリーはリポジトリにない。存在しない旧パスを正本にしない。
